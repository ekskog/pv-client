apiVersion: v1
kind: Service
metadata:
  name: photovault-frontend-service
  namespace: default
  labels:
    app: photovault-frontend
    component: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: photovault-frontend
