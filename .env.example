# PhotoVault Frontend Environment Configuration

# API Base URL - point to your PhotoVault API server
# For local development (if running API locally)
# VITE_API_URL=http://localhost:3001

# For production deployment with Kubernetes cluster
VITE_API_URL=https://vault-api.hbvu.su

# Authentication Mode: 'demo' or 'api'
# - 'demo': Use hardcoded credentials for development
# - 'api': Use database authentication via API endpoints
VITE_AUTH_MODE=demo

# Demo Mode Credentials (when VITE_AUTH_MODE=demo):
# Admin: admin / admin123
# User: user / user123
#
# For production, set VITE_AUTH_MODE=api and ensure your API
# server is configured with proper database authentication
